---
interface Props {
  value: string;
  bg_color?: string;
  text_color?: string;
}

const { value, bg_color, text_color } = Astro.props;
---

<input
  type="text"
  value=`${value}`
  disabled
  type="text"
  id="myInput"
  class="bg-[#F2F2F2] bg-black/5 h-[38px] dark:bg-white/5 w-full py-1.5 px-3 rounded-lg grid place-items-center text-slate-600 dark:text-slate-300"
/>
